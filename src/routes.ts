const routes = {
  home: (): string => "/",
  login: (): string => "/login",
  logout: (): string => "/logout",
  register: (): string => "/register",
  users: (): string => "/users",
};

export default routes;
